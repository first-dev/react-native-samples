import { SplashScreen } from "expo-router"
import "react-native-get-random-values"

export default function App() {
  // No need to show anything just keep the splash screen visible
  return <SplashScreen />;
}
